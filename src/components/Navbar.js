import React from 'react';

const Navbar = props => (
    <nav>
        <div className="nav-wrapper">
            <a href="/" className="brand-logo">NHL Picker</a>
        </div>
    </nav>

)

export default Navbar;